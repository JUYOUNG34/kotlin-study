package chapter01


/* fun main()이 실행 진입점
   변수는 기본적으로 val(불변)로 선언하고 , 값이 실제로 변해야 할 때만 var를 씀
   널 안정성은 코틀린의 핵심! 기본 타입은 널을 허용하지 않으며, 널 가능성을 표현하려면 String? 처럼 물음표 붙이기
   함수는 fun greet(name: String): String {...} 형태로 정의, 반복 사용되는 로직은 함수로 추상화
   단일 식 반환이라면 fun add(a: Int , b: Int) = a + b 처럼 본문을 줄일 수 있음
 */
class chapter01
{
}